// Source map schema + offset resolution (md offset -> body path + run offset). Built in M1.
export function resolveRange(_sourceMap, _mdStart, _mdEnd) {
  throw new Error("sourcemap: not yet implemented (M1)");
}
